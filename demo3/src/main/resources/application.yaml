spring:
  application:
    name: demo3
  datasource:
    url: jdbc:mysql://localhost:3306/demosql?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: mysql123
    driver-class-name: com.mysql.cj.jdbc.Driver
